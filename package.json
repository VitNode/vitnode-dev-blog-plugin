{
  "name": "vitnode_dev_blog_plugin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "start": "turbo start",
    "db": "turbo db",
    "lint": "turbo lint",
    "lint:fix": "turbo lint:fix",
    "docker:dev": "docker compose -f ./docker-compose-dev.yml -p vitnode-dev-vitnode_dev_blog_plugin up -d"
  },
  "overrides": {},
  "pnpm": {
    "overrides": {
      "react-is": "19.0.0-rc.1"
    }
  },
  "devDependencies": {
    "eslint-config-typescript-vitnode": "^0.0.10-canary.7",
    "turbo": "^2.3.0"
  },
  "packageManager": "pnpm@9.12.2",
  "workspaces": [
    "apps/*"
  ]
}