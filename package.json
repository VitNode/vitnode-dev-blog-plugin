{
  "name": "vitnode_dev_blog_plugin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "config:init": "turbo config:init",
    "dev": "turbo dev",
    "build": "turbo build",
    "start": "turbo start",
    "db": "turbo db",
    "lint": "turbo lint",
    "lint:fix": "turbo lint:fix",
    "docker:dev": "docker compose -f ./docker-compose-dev.yml -p vitnode-dev-blog up -d",
    "docker:prod": "docker compose -f ./docker-compose.yml -p vitnode-prod-blog up -d"
  },
  "overrides": {},
  "devDependencies": {
    "eslint-config-typescript-vitnode": "0.1.1",
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@9.12.2",
  "workspaces": [
    "apps/*"
  ]
}
